<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.aku.hassannaqvi.crt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SectionAActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SectionBActivity" />
        <activity android:name=".SectionCActivity" />
        <activity android:name=".SectionEActivity" />
        <activity android:name=".SectionFActivity" />
        <activity android:name=".SectionGActivity" />
        <activity android:name=".SectionDActivity" />
        <activity android:name=".SectionHActivity" />
        <activity android:name=".SectionIActivity" />
        <activity android:name=".SectionJActivity"></activity>
    </application>

</manifest>